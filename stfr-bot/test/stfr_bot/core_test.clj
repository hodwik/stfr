(ns stfr-bot.core-test
  (:require [clojure.test :refer :all]
            [stfr-bot.core :refer :all]
	    [clj-http.client :as client]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
